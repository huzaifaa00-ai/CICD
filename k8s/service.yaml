apiVersion: v1
kind: Service
metadata:
  name: cicd-demo-service
  namespace: devops-demo
spec:
  selector:
    app: cicd-demo-app
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  type: LoadBalancer
